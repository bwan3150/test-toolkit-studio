把一段内容写成文件交付给用户（存到脚本输出目录）。参数 `filename`=文件名（如 policy.md；只取文件名部分，不接受路径）；`content`=完整内容。用于"把 policy 存成 md""把读到的信息保存下来"等——通常先 explore 取回末页文字，你整理成 markdown 后用它落盘。**返回文件的完整绝对路径——务必把这个路径原样告诉用户**（用户就靠它找文件）；没成功拿到路径前别说"已保存"。
