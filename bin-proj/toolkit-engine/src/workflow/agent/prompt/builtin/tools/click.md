点击一个元素。

name 是这个**控件本身**的稳定语义名（它"是什么"），不是"你要做的动作"。它会作为可复用的测试资产落库，请用名词短语：
- 好：`KonecHome应用图标`、`导航栏个人中心入口`、`设置页退出登录按钮`、`确认退出弹窗的确认按钮`
- 坏：`打开KonecHome应用`、`进入个人中心`、`点击退出登录`（这些是动作，不是元素）

同一个控件在多轮里复用同一个 name。
