在**某个元素上**朝某方向滑动（区别于 swipe_direction 从屏幕中心滑）。用于：列表项左滑露出删除/更多按钮、拖动滑块/进度条、轮播图翻页、在某个可滚动区域内滚动等。
- element_id：要在其上滑动的元素序号。
- direction：滑动方向（如列表项 left=左滑）。
- amount：幅度=屏幕比例（full/half/quarter，默认 half）；或用 distance 给像素。

落 `定向滑动 [{元素}, 方向, 距离]`，回放时实时解析该元素位置，跨设备稳。
