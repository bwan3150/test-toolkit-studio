验证当前探索出的脚本：重启净化后整脚本回放，失败让脚本医生从失败步续接修复，再做稳定性测试（连续多次干净回放都到达目标才算稳定）。无参数，作用于「当前正在进行的那条用例」。只有 explore 达成后才有脚本可验证；探索未达成/被中断时此步无意义。返回验证摘要（是否稳定通过、修复次数、最终步数）。是否验证由你决定——常规流程是 explore 后做一次 verify 确认脚本能稳定回放，再 finalize。
