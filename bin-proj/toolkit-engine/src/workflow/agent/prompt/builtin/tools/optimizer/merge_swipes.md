把**一连串「定向滑动」盲滑**合并成一条可靠的「滚动查找」（盲滑回放受加载时机影响常滚不到位，滚动查找滚到目标文字出现为止、稳）。
- from / to：这段滑动的起止步号(1-based，含)；**from..to 之间必须全是「定向滑动」**。
- target：这段滑动结束后页面上会出现的、下一步要操作的**目标可见文字**。
- direction：滚动方向（看 .tks 里的方向，通常 up）。
- reason：为什么这段是盲滑、合并更稳。
系统会把 from..to 替换成 `滚动查找 ["target", 方向]`。
