把设备开到某个状态/页面（登录态、某个页、回首页）——**轻量导航，不产脚本、不写元素包、不做断言**，返回"到了没 + 当前页面摘要"。

用在：回放前对齐起始态（replay 报"起始页不符"时）、修复/稳定性测试前把设备复位、任何"先去到某状态"的准备动作。**导航是手段不是任务**：navigate 完立即回主线（通常 = replay 你要验证的那个脚本），不要顺手开新探索。

它自带纪律：走最短路径、不做改变账号/数据状态的操作（除非 goal 明确要求）。需要账号密码等信息就写进 note。要**产出脚本**的场景别用它，用 explore。
