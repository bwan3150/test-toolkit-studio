维护一张对用户可见的计划清单（todo list），把你打算走的步骤列出来并随进度勾选。参数 `todos`=完整清单（每次都传整张，不是增量），每项含 `text`（这一步做什么）和 `status`（pending 待办 / in_progress 进行中 / completed 已完成）。用法：接到一条用例先用它列出计划（例如"探索登录流程 / 验证脚本稳定性 / 收尾定稿"），开始某步前把它标 in_progress、做完标 completed，让用户随时看到进展。计划有变（用户改主意、需要重探）就更新整张清单。这是给用户看的进度视图，不影响工具的实际执行。
