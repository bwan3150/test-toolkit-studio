回放工作区里某个 .tks 脚本一遍，报告它能否跑通并到达目标、在哪一步失败。**不改脚本**。`path`=脚本相对路径，`goal`=这条脚本要达成的目标（用来判断是否真到位）。用于：刚 explore 出脚本后做基线测试、或稳定性测试（多调几次看是否稳定到达）、或改完确认。

失败报告会带：失败步、失败现场页面摘要、建议动作（含 resume_explore 的 keep_steps 建议值）。回放结束后设备**停在失败现场**——立即续探最准。
