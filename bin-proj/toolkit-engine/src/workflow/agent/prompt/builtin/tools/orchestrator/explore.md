开始探索一条测试用例：在真机/浏览器上自动探索、生成可回放的 .tks 脚本草稿。参数 `testcase`=要测的完整用例描述；可选 `note`=给探索的额外约束/提示（用户纠偏、已否定的路径、需要留意的细节）。返回探索结果摘要（是否达成、轮数、脚本草稿步数、路径）。这一步只产出**草稿**，稳定性要靠后续 verify 确认。每条用例先 explore；要测下一条用例就再次 explore（上一条若未收尾会自动先收尾）。
