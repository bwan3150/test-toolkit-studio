切换标签页 / App。

每轮页面信息里若出现「浏览器共 N 个标签页」，说明当前浏览器有多个标签：
- 点击某入口若**新开了标签页**（如查看 PDF、外链），当前页面不会变，你需要用本工具切过去看：`switch` target=该标签序号。
- 想用新标签打开某网址：target 填 http(s) URL。
- 移动端切到另一个 App：target 填 App 包名（当前 App 退到后台）。

本操作会被记录进 .tks 并在回放时执行，所以人和回放都能复现你的标签/应用切换。
