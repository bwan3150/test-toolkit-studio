看图视觉选元素：当 reexplore 给的元素列表里**没有**目标、或之前反复点同一个元素都**没反应**（很可能是纯图标元素、或目标被滑出屏幕外没在当前可视区）时，看 reexplore 那帧截图，直接给目标在截图中的像素框 region=[x1,y1,x2,y2]（优先，越贴合越好）或点击点 (x,y)。系统按像素裁 img 模板存库，并把该步改成对它的 action（默认 click）。input 动作再给 text。必须先 reexplore 定位、看到截图后才能用。
