请结合每步页面判断**真正开始跑偏的那一步**（常比报错步更早），用最小改动修好它：能删冗余/改一行参数就别 reexplore，确实需要点全新元素/走全新路径才 reexplore。改完用 run 看效果。
注意：若失败步是**断言（踩实校验）** 或 **滚动查找**，根因几乎都在**上游某步导航没真正进到该页**——去 reexplore 修那步导航，别删断言/滚动查找、也别改它们的目标来骗过去。