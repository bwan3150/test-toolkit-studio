任务目标：{case}

下面是任务**开始执行前**设备当前页面的全部可读文字（这个页面是脚本的**起始前提**——回放必须从这个状态开始，否则会越跑越乱）：
{page}

请从上面的文字里**原样摘取**一段「能认出这个起始页面」的独特文字，作为回放前的起始校验标志：
- 必须是上面出现过的**原文片段**——别改写、别缩写、别发明；
- 优先挑专属性强的标题/标签/名称；避开时间、计数、余额等每次会变的内容。

用 report 工具提交结果；实在挑不出独特文字就提交空字符串。
