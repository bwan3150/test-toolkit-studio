（重要：你上一步操作后，页面与操作前**完全相同**（已连续 {n} 次）。**不要再重复同一个操作**！

**如果上一步是点击却没让页面变化**：那次点击很可能**点错了元素 / 没真正生效**——典型是点了顶栏悬停菜单的同名项（点了不跳转，要点展开后的真正分类链接）、或点了个装饰性元素。请**优先在当前页换一个更对、更具体的元素/链接再点一次**（看看刚才那次点击有没有展开出新的可点项），**别急着滚动离开**——一滚走，这页其它还没试过的元素就错过了。

其它可能：①它触发了下载（下载不改变页面，做一次就够，别再点）；②在弹层中打开了；③若刚在滑动：很可能**方向反了**（想往下看内容要用 up，不是 down）或已到顶/到底——换个方向试。

请据此换一个**完全不同**的目标/做法（首选：本页换个更对的元素），或 request_screenshot 看截图，或如果该步已完成就继续下一步/finish。）
