脚本已定稿。下面这些元素被最终脚本用到，目前是探索时按特征自动起的临时名。请给每个起一个**稳定、可读的语义名**（名词，描述该控件"是什么"，便于以后复用和人读），如 登录按钮、个人中心入口、文档下载图标。

元素（临时名（文字/特征；desc））：
{elements}

只返回 JSON：{"临时名":"语义名", ...}，每个临时名给一个语义名；语义名不要含 { } [ ] 逗号。不要其它文字、不要 ``` 围栏。
