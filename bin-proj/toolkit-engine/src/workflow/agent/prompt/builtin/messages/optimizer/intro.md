整体测试目标：{case}
目标标志（只有真到达目标才会出现的文字）：「{marker}」

脚本逐步 trace（步号 · 成败 · 动作 · 该步执行后页面）：
{trace}

页面访问/重复分析（同一页被多步反复访问 = 很可能原地打转/绕路）：
{pages}

**可删的无效操作步**（执行后**页面结构没变化**、且后面没有断言确认它有效 = 点了个不起作用的元素，删了安全的步号）：{noops}
（这些就是"点了没反应的无效点击"——典型是回放 trace 里一个点击后**没跟断言、紧接着又是另一个点击**，说明上一个点击没生效。不在此列的步都改变了页面、或被随后的断言确认有效（如点击开新标签→下一步断言新标签内容）、或是承重步，删不得。）

编号脚本：
{script}

现在开始，每次只调用一个工具（delete_step / merge_swipes / done）。
