任务目标：{case}

任务已成功完成。下面是**完成后停住的最终页面**上的全部可读文字：
{page}

请从上面的文字里**原样摘取**一段「只有这个最终页面才会有」的独特文字，作为回放校验的目标标志：
- 必须是上面出现过的**原文片段**——别改写、别缩写、别发明；
- 优先挑专属性强的标题/标签/名称；避开时间、计数、余额等每次会变的内容；
- 注意目标标志校验的是**最终停住的页面**：如果用例以登出/关闭/返回收尾，那最终页面就是收尾后的页面（如欢迎页/登录页），标志必须属于它，而不是中途到过的"成就页"。

只返回 JSON：{"goal_marker":"..."}，不要 ``` 围栏或多余文字。实在挑不出独特文字就返回 {"goal_marker":""}。
