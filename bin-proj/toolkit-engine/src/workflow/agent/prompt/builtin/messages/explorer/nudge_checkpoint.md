你刚点击进入了新页面/菜单——先别急着继续点。请**先用 assert** 断言一个**该页独有、稳定**的标志元素（如页面标题、分类名、产品型号/编号、文档 SKU），把"确实到达了这一页"踩实下来：
- 挑该页**独有**的元素，别挑导航栏/页脚这种每页都有的通用控件；
- 这样回放时一旦上一步点错、进了别的页，会**在这条断言处立刻失败**，而不是带着错误一路往下点、到很后面才暴露、让问题极难定位。

断言完这一步，再继续做下一个操作。
