先别急着结束。请对照用户的**原始需求**，核对你现在所在的页面/状态是否真的达成了它。
用户原始需求：{case}
当前页面元素（[序号] 描述 @(中心坐标)）：
{page}
这个页面/状态是否真正满足了用户需求（到了对的目的地、做成了对的事，而不是相近/错误的页面）？

**收尾动作不参与判定（重要）**：用户需求里位于**最后**的「关闭浏览器 / 退出 App / close」这类**收尾(teardown)动作不是测试内容**——它必然成功，且一旦关闭页面就空了、根本无从验证。判定只看前面的**实质目标**：只要实质目标（如已打开并看到目标 PDF / 到达目标页）已达成，就判 **done=true**，**绝不要**因为「还没关闭浏览器/App」而打回。同理，目标页若是 PDF / 新标签页这类**解析不出元素**的内容页，只要已切到它、能看到其内容，就算达成，别再为了「确认」反复切标签页。

只返回 JSON：{"done":true/false, "reason":"..."}；done=false 时 reason 说明还差什么、下一步该干啥。不要其它文字、不要 ``` 围栏。
