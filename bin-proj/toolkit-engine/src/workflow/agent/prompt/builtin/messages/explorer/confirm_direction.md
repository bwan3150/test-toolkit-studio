（你上一步的操作**让页面变化了**——说明它有反应。但**变化≠方向正确**：可能进对了页，也可能只是点开了无关的菜单/弹窗、或进错了页。请先**确认方向再往下走**：
- 从下面这些**因上一步操作而新出现的元素**里，挑一个该页**独有**的，用 assert（图标类用 assert_visual）把它踩实——证明你确实到了预期的地方；
- 如果这些新元素表明你**进错了地方 / 只是展开了个无关菜单**，别往下走：back 回去，换一个更对的元素重试上一步。

因上一步操作而新出现的元素（优先从中挑一个 assert 确认方向）：
{delta}）
