用户的整体测试需求（判断"方向对不对"的依据）：
{case}

探索 agent 刚做的导航操作：{action}

当前页共 {total} 个元素，其中**因这次操作新出现**的 {news} 个如下（序号是各元素在当前页元素列表里的真实序号；**你只能从这里选**）：
{current}

请从上面挑一个最能证明"这次操作有效、确实进到了预期新页面/新状态"的标志元素，按系统提示词输出 JSON（index = 列表里那个元素的序号，+ reason）。挑不出能证明到位的标志就 index=null。
