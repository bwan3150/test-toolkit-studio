用户的整体测试需求（判断"方向对不对"的依据）：
{case}

探索 agent 刚做的导航操作：{action}

【点击前那一页的全部元素】（供你对比，不要从这里选）：
{prev}

【点击后当前页的全部元素】（**标 ★新 的是因这次操作新出现的**；你要从这一页选，返回它的序号）：
{current}

请对比前后两页，挑一个最能证明"这次操作有效、确实进到了预期新页面/新状态"的标志元素（优先从 ★新 的里挑），按系统提示词输出 JSON（index = 当前页里那个元素的序号，+ reason）。挑不出能证明到位的标志就 index=null。
