探索已达成目标。接下来我要**自动验证并修复**这个回放脚本：反复回放并校验是否真到达目标。请给我一段**只有真正达成目标时最终页面上才会出现**的独特文字（从你看到的目标页里挑，如文档标题/编号/SKU），用于自动校验是否到达目标，**务必准确、独特**。
已执行步骤：
{listing}

只返回 JSON：{"goal_marker":"..."}，不要调用工具、不要 ``` 围栏或多余文字。整体测试目标：{case}