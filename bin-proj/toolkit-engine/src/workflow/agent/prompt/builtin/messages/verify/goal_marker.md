探索已达成目标。接下来我要**自动验证并修复**这个回放脚本：反复回放并校验是否真到达目标。请给我一段**只有真正达成目标时最终页面上才会出现**的独特文字（从你看到的目标页里挑，如文档标题/编号/SKU），用于自动校验是否到达目标，**务必准确、独特**。注意校验的是脚本跑完后**最终停住的页面**：若用例以登出/关闭/返回收尾，标志必须属于收尾后的页面（如欢迎页/登录页），而不是中途到过的页面。没把握就返回空串（宁可不校验，也别给错误标志——它会诱导修复流程删掉正确的收尾步骤）。
已执行步骤：
{listing}

用 report 工具提交结果。整体测试目标：{case}