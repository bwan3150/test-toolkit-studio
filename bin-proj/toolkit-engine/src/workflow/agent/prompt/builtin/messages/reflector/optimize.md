测试目标：{case}
目标标志：「{marker}」

当前脚本**已能正确跑到目标**。它的逐步执行 trace（步号 · 成败 · .tks 行 · 该步后页面）：
{trace}

页面访问/重复分析（同一页被多步反复访问 = 很可能在原地打转/绕路，即使指令本身不重复也是冗余）：
{pages}

编号脚本：
{script}

请找出**可以删掉以缩短路径 / 提速**的步骤——重复点击、走错绕回又绕回来、点了没用的空步、连续多余的滑动等。
只返回 JSON：{"removable":[要删的步号,...], "note":"一句话说明删了哪些、为什么"}；不要其它文字、不要 ``` 围栏。
可以大胆指出（删错了系统会让医生在下一轮兜底修复），但**别删通往目标的关键步**。没有可删的就返回 {"removable":[]}。
