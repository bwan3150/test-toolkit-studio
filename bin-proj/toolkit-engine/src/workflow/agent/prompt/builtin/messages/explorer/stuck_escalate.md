【升级求助】你已连续多轮没有实质进展——继续换姿势乱试只会浪费轮次。现在**必须**二选一：
1. 调用 ask_user 求助：说清你卡在哪个页面、要找什么、已经试过什么（交互模式会转给用户；托管模式由全局视角的参谋给你方向）。能想到候选方向就带上 options。
2. 如果你判断当前路径根本走不通（功能不存在/权限不够/页面异常），调用 finish(success=false)，reason 里写清**卡点报告**：所在页面、试过什么、疑似缺什么——上层会拿着这份报告和用户讨论后带指导重来。
不要再直接执行下一个试探性操作。
