目标：{case}

这一轮探索**没能到达目标**就到上限了。它走过的全程（步号 · 动作 · 理由 · 该步所在页面的元素）：
{journey}

请结合**它实际走过的每个页面**复盘，然后产出一份**修正后的重探计划**（给下一轮探索照着走）：

1. **诊断（一两句）**：它在哪几步绕路 / 打转 / 进错页 / 钻牛角尖了（点出步号段 + 为什么）。
2. **修正后的路线计划（编号步骤，这是重点）**：根据它看到过的页面，给出这次该**怎么一步步走到目标**——从哪个入口进、进哪个类目/页面、在哪里滚到底找什么、到了目标页停在哪。每步一句、具体可执行，像给人指路一样；避开已被证明无关的地方。
3. 提醒重探时**每进入一个新页面/菜单先 assert 踩实**该页独有元素，避免再次悄悄进错页、带着错误一路走到底。

只输出"诊断 + 编号计划 + 那条提醒"，简明扼要、不要逐字复述全程。
