发一个原始 HTTP 请求并拿回响应（状态码、响应头、响应体摘要）。这是你顺藤摸瓜的主力：
拉 JS bundle、构造对后端 API 的探测、坐实某个敏感路径的内容，都用它。
请求与响应会自动落证据。4xx/5xx 会照常返回给你（不是错误），默认不跟随重定向。
参数：method（GET/POST/…）、url（必须在目标同源范围内，除非只是观察第三方）、
headers（可选，键值对数组）、body（可选，原样发送）。
