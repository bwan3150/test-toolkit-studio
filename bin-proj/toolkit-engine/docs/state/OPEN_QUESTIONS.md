# 未决问题

只增删,不重写。格式:编号 / 提出时间 / 问题 / 阻塞什么。

## Q-1 (2026-07-13) 分诊层 2-5 真机质量未知
replace/wrong_page/path_changed/app_issue 只有 fake 测试,真实改版 App 上判断质量未验。
阻塞:对外宣称"改版不断档"的可信度。需要拿真实改版场景逼出。

## Q-2 (2026-06-25) 探索质量债(部分已修未复验)
web 小图标(eye/查看)落点识别、滚动查找关键词/方向策略、token 爆炸(横跳止损已做)、
工具/提示词按平台全面分类(只开了 hover 一个 gate)。见记忆 explore-doctor-quality-todo。

## Q-3 (2026-07-08) 真机 TUI 结果框/todo 行交错渲染错乱
疑 resize 或双 Summary 竞争,未复现未修。

## Q-4 (2026-07-06) wda/web 是否有 adb 同款"无限挂"风险 —— ✅ 已关闭 (2026-08-15)
adb 全链路超时已加(P-03),wda/web 未审计。

**结论:没有同款风险。** 审计结果:`web`/`wda` 全部 **17 处 ureq 调用 1:1 都带 `.timeout()`**
(web 的还统一收在 `get()`/`post()` 两个函数里),所有等待循环都是有界的 `(0..N).any(…)`、
无裸 `loop{}`。根本差异:**adb 是 spawn 子进程,没有任何自带超时**(所以 P-03 必须全链路兜底);
web/wda 走 HTTP,ureq 自己就会超时。外部进程(chromedriver/go-ios)是 spawn 后台化 +
有界轮询探活,不阻塞调用线程。残余风险仅剩 `pgrep`/`pkill`/`kill` 这类瞬时本机命令,可忽略。

## Q-5 (2026-07-13) 60s 步超时与对齐本地匹配准确率
真死页面要等 60s;本地匹配误判"不在起始页"会白烧一轮导航。待真机数据。

## Q-6 (2026-08-12) 两件套「拷走即跑」还差平台自包含 —— ✅ 已关闭 (2026-08-13)
`.tks` 不记平台,`tke run foo.tks` 不带 `-d` 时按 Android 推断 → web 脚本报「adb 缺失」。
但 `foo.tklib` 的 meta.json **已经存了 platform**。是否让 run 在缺 `-d` 时从同名 tklib
读 platform 兜底(web→device="web";android→留 None 走默认设备;ios 的 UDID 不可照搬,仍需用户给)?
阻塞:INV-7「拷到别的机器直接能跑」目前还差这一口气。skill 实测撞出。

**结论**:按上述方案原样实现(用户 2026-08-13 拍板)。ios 额外在报错里回显录制 UDID。
本机无头实测:不带 `-d` 回放 web 两件套,2/2 步、退出码 0。见 CHANGELOG 同日条目。

## Q-7 (2026-08-12) 跨设备测试还缺两块
flow per-script device 已落地(表达得了「A 手机改设置 → B 手机验收」),但:
① **数据传递**:A 上产生的动态值(订单号/验证码)传不到 B 的断言里——`.tks` 没有变量。
   固定已知值的场景不受影响,动态值是硬伤。
② ~~**最终一致性**:下发有延迟,B 上验收需要「轮询到出现或超时」~~ **✅ 已做**(2026-08-12):
   `断言 [{提示}, 存在, 10s]` 重试断言已落地,步超时对自带时长的命令一并放宽。
阻塞:剩下的是 ①。变量放最后(它会把 .tks 推向编程语言)。

## Q-8 (2026-08-13) skill 侧缺「不依赖元素库的等待」
`.tks` 的重试断言 `断言 [{元素}, 存在, 15s]` **需要元素库**,而 tke-ui-test skill 明令不建元素库
(ADR-0010:只做一次性检查,不产资产)。于是跨设备等下发只能让调用方 AI 自己写 shell 轮询
(`for i in $(seq ...); do tke -d X fetch | grep -q ...; done`)——已写进 SKILL.md 并实测可用,
但**护栏全靠提示词**:忘超时、忘判断命中、误用 `--interactive` 都会产生假结论(我自己第一版就
误用了 `--interactive`)。
是否加 `tke fetch --wait-text <文本> --timeout <秒>`(轮询现有采集 + 文本匹配,实现很轻)?
ADR-0010 埋的坑那节写过:**护栏退化的出路是做成必须调用的子命令,不是把提示词写更长**。
阻塞:跨设备检查的可靠性。等用户 mac 实测一轮再定——若 AI 照提示词就能写对,可以不加。

**✅ 已关闭 (2026-08-15):加了**(ADR-0013)。没等 mac 实测就定,因为 Q-9 这场给了独立佐证:
`滚动查找` 的能力一直都在、也不需要元素库,但文档把它标成"需要元素库",
**于是调用方 AI 一次都没用过它**——靠文档传递能力是脆的,靠退出码传递结论是硬的。
本机实测:命中 0.45s 退出 0 / 超时 5.45s 退出 1。所有手写轮询范例已从 skill 文档移除。
**跨设备真机仍待验**(本机无第二台设备)。

## Q-9 (2026-08-15) 语义定位 + 提速的实际收益没量过
用户实测反馈"token 太大、等待太多"后做了三刀:①SKILL.md 从坐标掉头到 `点击 ["文字"]`
②文字定位补隐式等待、文档明确"默认别写等待" ③修 P-27(每步 4899ms → 750ms)。
**但没有人用新版重跑过那个跨端任务**——预期批次数 20 → 个位数、总耗时 3.8 分钟 → 半分钟级,
这只是推算。阻塞:不知道 AI 会不会仍然习惯性 fetch 算坐标(那说明 SKILL.md 措辞还不够硬)。
做法:让用户用同一个场景重跑,对比新旧报告的批次数/步数/等待占比。

**🟡 部分回答 (2026-08-15):自己当调用方 AI 在本机 web 上实跑了一遍**,结论比预想的重要——
**语义定位这条路当时是断的,不是 AI 不听话**。一个普通链路撞出四个洞(全部已修,见 CHANGELOG):
①`sr-only` 1×1 幽灵元素抢走文字定位(P-28) ②输入框的可及名称(`<label for>`)根本没被采集
③点空后的报错「请先点击输入框」把 AI 引回坐标路线 ④视口外目标必失败,而破解它的
`滚动查找` 被文档写成"需要元素库"(P-30)。修完:同一批 3 步**纯语义、零坐标、零 fetch** 通过。
另量到:**一次 `fetch --interactive` = 32KB ≈ 8K token**(fetch 本身只要 237ms)——
**贵的是那张表,不是时间**。
**仍待用户做的**:①跨端(安卓侧)本机无设备,没验 ②他那个真实业务场景重跑一次,
对比新旧报告的批次数/步数——这才是 Q-9 原本要的那个数。
⚠️ **重跑前必须先重装 skill**(见 Q-11),否则拿到的还是旧文档、必然得出"没改善"。

## Q-10 (2026-08-15) 移动端有没有同款"白等"
P-27 修的是 **web 驱动**(`wait_ready` 与 `center_into_viewport` 多解一层 value)。
adb / wda 两条驱动**没查过**有没有类似的静默退化或固定 sleep。
用户那次跨端任务里安卓侧同样慢,值得用同样的办法量一遍(单步耗时 vs 单次采集耗时)。

**🟡 静态审计已做 (2026-08-15),真机量待用户**:
- **没有** P-27 那种"读不到值→等满"的静默退化:`adb`/`wda` 的 `tap` 后**根本不等**
  (只有 web 有 `wait_ready`)。wda 的操作路径上没有任何固定 sleep(只在 infra 启动路径上有)
- 反而查出**反向**同族:web 吃了移动端才需要的 500ms 软键盘等待(**P-29,已修**,占该步 ~38%)
- **安卓侧"慢"的最大嫌疑是采集**:每次 `capture_ui_state` 要 **6 次 adb 进程往返**
  (`screencap`+`pull`+`rm`、`uiautomator dump`+`pull`+`rm`)。`exec-out` 可以把截图那三次并成一次,
  dump 那三次也能减到两次(`get_ui_xml` 已经是 `dump`+`cat` 两次的写法)
- **没改**:本机无安卓设备,量不了。按 P-27 的教训**先量再改**——盲改驱动是这个项目最贵的错误
- **量法(用户在真机上跑)**:`tke -d <序列号> steps '点击 [{x,y}]' --log <dir>` 看 `log.json`
  的 `duration_ms`,与单次 `time tke -d <序列号> fetch` 对比。**单步远大于单次采集 = 有黑洞**,
  这正是 P-27 被逼出来的方法


## Q-11 (2026-08-15) 装好的 skill 过期了,没有任何东西会告诉用户
这场一开始就撞到:本机 `~/.claude/skills/tke-ui-test/` 里躺的是 **8-13 的旧版**(8.7KB),
而 repo 里是 8-15 的新版(15.6KB)——正好差了「语义定位掉头」和「提速」这两刀。
**若用户直接重跑 Q-9 那个场景,他的 AI 拿到的还是坐标路线的旧文档,必然得出"没改善"。**

更新链路本身是有的(`install.sh` 幂等,重跑即覆盖),缺的是**过期提示**:
- `tke fix --check` 只体检依赖,不看 skill 版本
- SKILL.md 里没有版本戳,人和 AI 都看不出装的是哪天的
- 分发源有 `VERSION`(带 build 戳),install.sh 会读——但只在装的时候读

可选做法:①SKILL.md 顶部写版本/日期戳,`tke fix --check` 顺带回显本地 skill 版本(不联网)
②`--check` 联网比对分发源 VERSION 并提示"有新版,重跑 install.sh"(要注意 ADR-0012:
`tke fix` 是唯一联网**下载**的命令,只读版本号算不算破例需要拍板)
③装的时候把版本写进 skill 目录下一个小文件,给人一个能 `cat` 的东西

阻塞:**每次改完 SKILL.md 的收益都可能到不了用户手上**——这一场的四个修复同样会卡在这里。
优先级建议:高(它让所有 skill 侧改进的交付变得不确定)。

**✅ 已关闭 (2026-08-17)**:按 ADR-0014 落地(用户拍板了三个选择:改名 doctor / 缓存 4h / 只提醒)。
- `tke fix` → **`tke doctor`**(体检,不下载);`doctor --fix` 才下载;`tke fix` 与 `fix --check` 保留为别名
- **比 `build` 戳而不是版本号**——版本号只在 bump 时才变、SKILL.md 却天天改,这正是当初
  `--check` 说"一致"却抱着旧文档的原因。`publish.sh` 把 VERSION 一起打进 skill 包
- **`tke steps` 每批都问**(结果缓存 4h、超时 5s、打 stderr、`--json` 闭嘴)——
  指望人想起来跑体检正是踩坑的原因
- 发现不一致**只提醒 + 给命令**,不自己覆盖二进制(三平台各有各的坑,install.sh 已踩平)
- 实测:模拟 08-13 的旧 skill → doctor 与 steps 都如实报出并给更新命令;
  没有版本文件的老安装 → 报"没有版本信息",**不误报过期**

## Q-12 (2026-08-19) iOS **真机**的 WDA 还要人工过一次 Xcode

模拟器已经全自动（`doctor --fix` 下预编译 runner，tke 自己 `simctl install/launch`）。
真机卡在**签名**：

| | 能不能自动 |
|---|---|
| 证书（Apple Development） | ✅ 本机 Keychain 里有就能用（`security find-identity` 查得到） |
| **provisioning profile**（必须含目标设备 UDID） | ❌ **生成它必须联 Apple**，要开发者账号凭据 |

tke 不该去碰用户的 Apple ID。所以真机能砍掉的是「clone + 编译 WDA 工程」（我们分发未签名产物、
自动 `codesign` + `go-ios install`），**砍不掉「让 Xcode 往那台设备装一次任意 App 以生成 profile」**。

要做的前提：用户机器上有证书**且**有覆盖目标设备的 profile。已知用户那台有 3 个 Apple Development
证书，profile 目录待确认（Xcode 16+ 挪到了 `~/Library/Developer/Xcode/UserData/Provisioning Profiles/`）。

## Q-13 (2026-08-19) 模拟器端口写死 8100，多台并行会撞 —— ✅ 已关闭 (2026-08-20，**用户实测通过**)

模拟器与主机共享网络，所有实例的 WDA 都监听 8100。单台够用，**并行跑多台会互相抢**——
抢输的那台起不来还算好的，更糟的是端口通、命令却发到了另一台上（每步都报成功，动的是别人）。

**做法**：每台按 UDID 定端口（`8100 + 1 + hash % 99`，**有意跳过 8100**——那是 WDA 出厂默认，
任何一台没带 `USE_PORT` 起来的都在那儿），启动时 `SIMCTL_CHILD_USE_PORT` 传给 runner，
带 `--terminate-running-process`（不杀旧的，launch 只是把它带到前台，USE_PORT 不生效）。
复用前**核对端口归属**：`simctl spawn <udid> launchctl list` 的 PID = `lsof` 的监听 PID。

**用户实测（2026-08-20，iPhone 17 Pro + iPhone 16 Pro 并行）**：
端口 8149 / 8197 分开、两边 PID 都对上、两个端口报的前台是不同的 App、两边证据各落各的目录。
**那个预编译 WDA runner 认 `SIMCTL_CHILD_USE_PORT`**——这条之前只是推测。

过程里逼出三个洞（都已修）：
1. 端口从状态文件继承 → 旧状态里两台都是 8100，于是又都挑了 8100
2. 归属校验用 bundle id 精确查 `launchctl` → iOS 的 label 是
   `UIKitApplication:com.facebook.…[0x…][rb-legacy]`，**永远查不到**，校验一路放行，
   第二台直接复用了第一台的 WDA（正是要防的那件事）
3. `ensure_existing` 吞掉 attach 的真实错误（见 PITFALLS 新增条目）

验法：`bash scripts/verify-sim-parallel.sh <UDID-A> <UDID-B>`（不给参数会列出可选的）。
判串台**不能比分辨率**（iPhone 17 Pro 与 16 Pro 都是 1206×2622），
也**不能用 `device info`**（机型问的是 simctl，不经过 WDA）——要给两台开不同 App，
再问每个端口「你前台是谁」。

## Q-14 (2026-08-19) `tke harness` 在四端的实跑没验过

驱动层四端都通了（`tke steps` / `fetch` 用户在 web + iOS 模拟器上验过），
但 **harness 的 AI 探索闭环**只在 web 上跑过。它用的是同一套驱动，理论上换 `-d` 就行，
但「AI 看着 iOS 元素表能不能做对决策」是另一回事（XCUI 的 `resource_id` 往往就是可见文字，
跟安卓/web 的语义不同）。需要有 AI key 的环境实跑。

## Q-15 (2026-08-25) tke security 对话式 TUI 交互手感真机未验

`tke security` 无参进 TUI → 主 agent 开场面试（目标/强度/scope，选项选择）→ 探测 → 出报告，
逻辑链路真机跑过一部分（面试 + 探测 + 阶梯缩进已修），但**完整交互手感未系统验**：
选项列表方向键选、追问节奏、用户中途插话/改方向、`report` 时机。只有真 TTY 能暴露——
上一场 prober 死循环就是真机才逼出来的。**待用户在 mac 上实跑一遍 `tke security` 对真实产品做一次深挖。**

## Q-16 (2026-08-25) service-playbook 覆盖面

现覆盖 Sanity/Firebase/Supabase/S3/Algolia/Hasura。真实世界还有 Contentful/Strapi/Directus/
Elasticsearch/MongoDB/Redis/公开 Swagger 等。**扩法已固定**：playbook 加一条 + `recon detect` 加一个正则，
不改架构。按遇到的真实站点按需加，不必一次堆全。
