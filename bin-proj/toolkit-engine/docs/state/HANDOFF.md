# 交接单

**会话时间**: 2026-08-26～27（第九场：**平台全链路跑通 + 内网节点**）
**产出 commit**: `9c369d49`（平台字段/三处假绿）→ `df105e52`（反向通道+多模拟器）→ `76ed4688`（Skill 落点）→ `d6f17004`（删解说词）→ `5535ea72`（第二台 AVD）→ `e7e27a49`（rustls panic）→ `2b94560c`（没跑的测试）→ `29d70449`（INV-18）→ `a71a9946`（设备池重扫）→ `1e50119c`（AVD 指错 + physical）

平台侧在 `~/Documents/GitHub/TOOLKIT/bug`，见那边的 `docs/state/HANDOFF.md`。

## 一句话

服务化从「P3 能跑」做到了**真的在用**：真实的公网平台 + 内网 Arch 机器，
建用例 → 挂两件套 → 组套件 → 下发回放 → 回写结果 → 产物落对象存储，3/3 通过。

## 这一场的实质

**几乎所有工作量花在「实跑逼出来的洞」上，不在写新功能。** 新功能只有两件
（反向通道 ADR-0024、多台模拟器），其余全是修 —— 而每一个都是**跑起来之前
看不出来的**：编译过、单测绿、代码读着也对。

值得下一场记住的模式：

| 洞 | 为什么之前发现不了 |
|---|---|
| 缺 `步骤:` 的脚本零步跑完报成功 | 判据 `成功数==总数` 在 0/0 时恒真 |
| 平台选了 web、节点去租 android | 错误信息如实描述了它做的事，只是没说"我为什么找 android" |
| 缺 AI 配置被判成"用例没通过" | done.success=false 一律 failed，错误播报被丢 |
| 写进去了读回来没有 | 迁移和写入都做了，**读取路径没人动** |
| 连 https 平台 panic | 本地一路测 http，**验证时用的协议跟真实用法不一样** |
| 第二台 AVD 建了认不出 | 参数化漏了一处；而"已装"的判据只看文件在不在 |
| 设备池启动扫一次就定死 | `set_pool` 这个函数在，但**没有任何地方调用它** |

## 硬规矩（新增）

**INV-18 CLI 输出不解释自己**：终端每一行要么是结果、要么是下一步能敲的命令。
判据是「这句话会不会改变他下一步做什么」。需要解释的写进 `docs/`。
错误信息不受限——它必须说清为什么失败、怎么办。

## 还没做的

- **Q-20 仍未关**：真跑一次 AI 编排要节点配 `[ai]`，本机没 key。
  L2 那条路（explore/security）**从没在真实 AI 下端到端跑过**
- **反向通道只在单副本下正确**（ADR-0024 已写明）。用户说暂时没多副本计划
- 节点部署文档在 `docs/node-deploy.md`（systemd + 升级要 restart + 踩坑清单）
- **用户下一场想做的：tke 链路的体验优化**

## 别再踩的

1. **验证用构建产物，而且用真实的协议/参数**。这一场踩了两次：
   加完 b64 只跑 `cargo build` 没出产物（节点跑的还是旧二进制，两件套推上去是 0 字节）；
   TLS 那次一路测 http，用户连 https 当场 panic
2. **`pgrep -f "tke serve"` 会匹配到发起命令的那个 shell**，`kill` 下去把自己杀了
   （退出码 144，踩了两次）。用 pidfile 或 systemd
3. **改完页面要打开看一眼**。平台那边两次都是没看：`--platform-token undefined`、
   节点名带空格没加引号
4. **编译器警告不是噪音**：`duplicated attribute` + `function is never used` 两条并排，
   意思是有条测试一直没跑
