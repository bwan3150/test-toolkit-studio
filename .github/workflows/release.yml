name: Build Release

# 触发条件: 手动触发或推送 tag
on:
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: 0.4.2)'
        required: false
        default: ''
      build_mac:
        description: '构建 macOS 版本'
        type: boolean
        default: true
      build_windows:
        description: '构建 Windows 版本'
        type: boolean
        default: true
  push:
    tags:
      - 'v*.*.*'

# 授予 GitHub Actions 写入权限以便提交 package.json 更新
permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  NODE_VERSION: '20'
  PYTHON_VERSION: '3.11'
  RUST_VERSION: 'stable'

jobs:
  # 准备工作: 获取版本信息并更新 package.json
  prepare:
    name: 准备构建信息
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      date: ${{ steps.version.outputs.date }}
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 获取版本号
        id: version
        run: |
          if [[ "${{ github.event_name }}" == "push" ]]; then
            VERSION=${GITHUB_REF#refs/tags/v}
            TAG=${GITHUB_REF#refs/tags/}
          elif [[ "${{ github.event.inputs.version }}" != "" ]]; then
            VERSION="${{ github.event.inputs.version }}"
            TAG="v$VERSION"
          else
            VERSION=$(grep '"version"' package.json | head -1 | sed -E 's/.*"version": *"([^"]+)".*/\1/')
            TAG="manual-$VERSION"
          fi

          # 确保 VERSION 不包含 'v' 前缀（安全检查）
          VERSION=${VERSION#v}

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "date=$(date +'%Y%m%d')" >> $GITHUB_OUTPUT

          echo "📦 版本: $VERSION"
          echo "🏷️  标签: $TAG"

  # macOS 构建
  build-macos:
    name: 构建 macOS 应用
    needs: prepare
    runs-on: macos-latest
    environment: aws-production  # 使用 Environment Secrets
    env:
      BUILD_VERSION: ${{ needs.prepare.outputs.version }}
    if: |
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.build_mac == 'true')
    steps:
      - name: 检出代码（跳过 LFS 大文件）
        uses: actions/checkout@v4
        with:
          lfs: false  # 不下载 LFS 大文件，从源代码重新构建

      - name: 更新 package.json 版本号（仅本地）
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          CURRENT_VERSION=$(grep '"version"' package.json | head -1 | sed -E 's/.*"version": *"([^"]+)".*/\1/')

          if [[ "$CURRENT_VERSION" != "$VERSION" ]]; then
            echo "本地更新 package.json: $CURRENT_VERSION → $VERSION"
            sed -i '' -E "s/\"version\": *\"[^\"]+\"/\"version\": \"$VERSION\"/" package.json
          else
            echo "package.json 版本已是最新: $VERSION"
          fi

      # 设置 Node.js 环境
      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      # 设置 Rust 环境
      - name: 设置 Rust 工具链
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}

      # 缓存 Cargo
      - name: 缓存 Cargo 依赖
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            tester-ai/target
            scrcpy-server/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      # 设置 Python 环境 (用于 opencv-matcher)
      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # 安装 uv (Python 包管理器)
      - name: 安装 uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      # 安装 Node.js 依赖
      - name: 安装 Node.js 依赖
        run: |
          npm install
          npm audit fix || true

      # tke 不再从源码构建 —— 它已拆成独立仓库（Test-Toolkit-Engine），
      # 那边的 CI 把六平台二进制发到分发源，这里取现成的。
      # 好处不只是省十几分钟编译：打包用的和用户 `tke update` 拿到的是同一份产物
      - name: 取 Toolkit Engine 二进制
        run: |
          echo "=== 取 TKE for macOS ==="
          ./scripts/fetch-tke.sh --remote darwin-arm64 --out resources/darwin/toolkit-engine
          ls -lh resources/darwin/toolkit-engine/tke
          resources/darwin/toolkit-engine/tke --version

      # 构建 opencv-matcher (Python)
      - name: 构建 OpenCV Matcher (Python)
        working-directory: opencv-matcher
        run: |
          echo "=== 构建 tke-opencv for macOS ==="
          echo "版本号: $BUILD_VERSION"

          # 生成 _version.py 文件（打包时嵌入版本号）
          echo "# 自动生成的版本文件 - 请勿手动修改" > _version.py
          echo "__version__ = '$BUILD_VERSION'" >> _version.py
          echo "✓ 已生成 _version.py: $BUILD_VERSION"

          # 同步依赖并打包
          uv sync --group dev
          .venv/bin/pyinstaller \
            --onefile \
            --name tke-opencv \
            --clean \
            --noconfirm \
            opencv_matcher.py

          # 清理生成的 _version.py
          rm -f _version.py

          # 复制到资源目录
          mkdir -p ../resources/darwin/toolkit-engine
          cp dist/tke-opencv ../resources/darwin/toolkit-engine/tke-opencv
          chmod +x ../resources/darwin/toolkit-engine/tke-opencv

          echo "✅ tke-opencv 构建完成"
          ls -lh ../resources/darwin/toolkit-engine/tke-opencv

      # 构建 tester-ai (Rust)
      - name: 构建 AI Tester (Rust)
        working-directory: tester-ai
        run: |
          echo "=== 构建 tester-ai for macOS ==="
          echo "版本号: $BUILD_VERSION"

          # 构建（build.rs 会自动从 BUILD_VERSION 环境变量读取版本）
          cargo build --release

          # 复制到资源目录
          mkdir -p ../resources/darwin/tester-ai
          cp target/release/tester-ai ../resources/darwin/tester-ai/tester-ai
          chmod +x ../resources/darwin/tester-ai/tester-ai

          echo "✅ tester-ai 构建完成"
          ls -lh ../resources/darwin/tester-ai/tester-ai

      # 构建 scrcpy-server (Rust)
      - name: 构建 Scrcpy Server (Rust)
        working-directory: scrcpy-server
        run: |
          set -e  # 遇到错误立即退出
          echo "=== 构建 scrcpy-server for macOS ==="
          echo "版本号: $BUILD_VERSION"

          # 构建（build.rs 会自动从 BUILD_VERSION 环境变量读取版本）
          cargo build --release

          # 创建目标目录
          mkdir -p ../resources/darwin/scrcpy-server

          # 复制二进制文件
          cp target/release/tke-scrcpy ../resources/darwin/scrcpy-server/tke-scrcpy
          chmod +x ../resources/darwin/scrcpy-server/tke-scrcpy

          # 直接复制 scrcpy-server.jar 到二进制文件旁边
          cp vendor/Genymobile/scrcpy/scrcpy-server.jar ../resources/darwin/scrcpy-server/scrcpy-server.jar

          echo "✅ scrcpy-server 构建完成"
          ls -lh ../resources/darwin/scrcpy-server/tke-scrcpy

          # 验证 jar 文件
          SCRCPY_JAR="../resources/darwin/scrcpy-server/scrcpy-server.jar"
          if [ -f "$SCRCPY_JAR" ]; then
            echo "scrcpy-server.jar: $(du -h "$SCRCPY_JAR" | cut -f1)"
          else
            echo "❌ scrcpy-server.jar 未找到"
            exit 1
          fi

      # 验证所有二进制文件
      - name: 验证构建产物
        run: |
          set -e  # 遇到错误立即退出
          echo "=== 验证构建产物 ==="

          echo "📦 TKE:"
          resources/darwin/toolkit-engine/tke --version

          echo "📦 tke-opencv:"
          resources/darwin/toolkit-engine/tke-opencv --version

          echo "📦 tester-ai:"
          resources/darwin/tester-ai/tester-ai --version || resources/darwin/tester-ai/tester-ai --help

          echo "📦 tke-scrcpy:"
          resources/darwin/scrcpy-server/tke-scrcpy --version || resources/darwin/scrcpy-server/tke-scrcpy --help

          echo "📦 scrcpy-server.jar:"
          ls -lh resources/darwin/scrcpy-server/scrcpy-server.jar

          echo "✅ 所有二进制文件验证完成"

      # 下载 FFmpeg 依赖（macOS）
      - name: 下载 FFmpeg
        run: |
          echo "=== 下载 macOS FFmpeg ==="

          FFMPEG_URL="https://toolkit-studio-updates.s3.ap-southeast-2.amazonaws.com/dependency/darwin/ffmpeg"
          TARGET_DIR="resources/darwin/toolkit-engine"
          TARGET_FILE="$TARGET_DIR/ffmpeg"

          # 确保目标目录存在
          mkdir -p "$TARGET_DIR"

          # 下载 ffmpeg
          echo "下载 ffmpeg 从: $FFMPEG_URL"
          curl -L "$FFMPEG_URL" -o "$TARGET_FILE"
          chmod +x "$TARGET_FILE"

          echo "✅ ffmpeg 下载成功"
          ls -lh "$TARGET_FILE"

      # 配置 macOS 代码签名
      - name: 配置 macOS 代码签名
        env:
          APPLE_CERT_BASE64: ${{ secrets.APPLE_CERT_BASE64 }}
          APPLE_CERT_PASSWORD: ${{ secrets.APPLE_CERT_PASSWORD }}
        run: |
          echo "=== 配置 macOS 代码签名 ==="

          # 创建临时 keychain
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          KEYCHAIN_PASSWORD=$(openssl rand -base64 32)

          # 创建并解锁 keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          # 导入证书
          CERT_PATH=$RUNNER_TEMP/certificate.p12
          echo "$APPLE_CERT_BASE64" | base64 --decode > "$CERT_PATH"
          security import "$CERT_PATH" -P "$APPLE_CERT_PASSWORD" -A -t cert -f pkcs12 -k "$KEYCHAIN_PATH"

          # 设置为默认 keychain 并允许 codesign 使用
          security list-keychain -d user -s "$KEYCHAIN_PATH"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          echo "✅ 代码签名配置完成"
          security find-identity -v -p codesigning

      # 构建 Electron 应用 (macOS)
      - name: 构建 Electron 应用
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ap-southeast-2
        run: |
          echo "=== 构建 Electron 应用 for macOS ==="
          # electron-builder 只在检测到 git tag 时才会上传到 S3
          npx electron-builder --mac

          if [[ "${{ github.event_name }}" == "push" ]]; then
            echo "✅ Electron 应用构建完成并已上传到 S3"
          else
            echo "✅ Electron 应用构建完成（未上传到 S3，仅测试构建）"
          fi
          ls -lh dist/

      # 清理 keychain
      - name: 清理 keychain
        if: always()
        run: |
          security delete-keychain $RUNNER_TEMP/app-signing.keychain-db || true

      # 重命名应用
      - name: 重命名应用
        run: |
          mkdir -p release

          # 查找生成的 DMG 或 APP
          if ls dist/*.dmg 1> /dev/null 2>&1; then
            DMG_FILE=$(ls dist/*.dmg | head -n 1)
            cp "$DMG_FILE" "release/Toolkit Studio ${{ needs.prepare.outputs.tag }} macOS.dmg"
            echo "✅ 找到 DMG: $DMG_FILE"
          elif ls dist/mac/*.app 1> /dev/null 2>&1; then
            APP_FILE=$(ls -d dist/mac/*.app | head -n 1)
            # 压缩为 zip
            cd dist/mac
            zip -r "../../release/Toolkit Studio ${{ needs.prepare.outputs.tag }} macOS.zip" *.app
            cd ../..
            echo "✅ 找到 APP，已压缩为 ZIP"
          else
            echo "❌ 未找到 DMG 或 APP 文件"
            ls -R dist/
            exit 1
          fi

      # 上传到 Artifacts
      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: macos-app
          path: release/*
          retention-days: 30

  # Windows 构建
  build-windows:
    name: 构建 Windows 应用
    needs: prepare
    runs-on: windows-latest
    environment: aws-production  # 使用 Environment Secrets
    env:
      BUILD_VERSION: ${{ needs.prepare.outputs.version }}
    if: |
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.build_windows == 'true')
    steps:
      - name: 检出代码（跳过 LFS 大文件）
        uses: actions/checkout@v4
        with:
          lfs: false  # 不下载 LFS 大文件，从源代码重新构建

      - name: 更新 package.json 版本号（仅本地）
        shell: pwsh
        run: |
          $VERSION = "${{ needs.prepare.outputs.version }}"
          $content = Get-Content package.json -Raw
          $currentVersion = ($content | Select-String -Pattern '"version"\s*:\s*"([^"]+)"').Matches.Groups[1].Value

          if ($currentVersion -ne $VERSION) {
            Write-Host "本地更新 package.json: $currentVersion → $VERSION"
            $content = $content -replace '"version"\s*:\s*"[^"]+"', """version"": ""$VERSION"""
            $content | Set-Content package.json -NoNewline
          } else {
            Write-Host "package.json 版本已是最新: $VERSION"
          }

      # 设置 Node.js 环境
      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      # 设置 Rust 环境
      - name: 设置 Rust 工具链
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}

      # 缓存 Cargo
      - name: 缓存 Cargo 依赖
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            tester-ai/target
            scrcpy-server/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      # 设置 Python 环境
      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # 安装 uv (Windows) - 使用 pip 安装更可靠
      - name: 安装 uv
        shell: cmd
        run: |
          pip install uv
          uv --version

      # 安装 Node.js 依赖
      - name: 安装 Node.js 依赖
        shell: cmd
        run: |
          npm install
          npm audit fix

      # 构建 toolkit-engine (TKE)
      # 同 macOS：取现成的二进制，不再编译源码（tke 已是独立仓库）
      - name: 取 Toolkit Engine 二进制
        shell: bash
        run: |
          set -euo pipefail
          echo "=== 取 TKE for Windows ==="
          ./scripts/fetch-tke.sh --remote windows-amd64 --out resources/win32/toolkit-engine

          # ADB 的 DLL 依赖（Windows 的 adb.exe 要它们）。
          # 路径从仓根算 —— 这一步以前有 working-directory: toolkit-engine，现在没有了
          for dll in AdbWinApi.dll AdbWinUsbApi.dll; do
            if [ -f "resources/win32/$dll" ]; then
              cp -f "resources/win32/$dll" "resources/win32/toolkit-engine/$dll"
              echo "已带上 $dll"
            else
              echo "注意：resources/win32/$dll 不在，adb 可能起不来"
            fi
          done

          resources/win32/toolkit-engine/tke.exe --version
          dir "..\resources\win32\toolkit-engine\tke.exe"

      # 构建 opencv-matcher (Python)
      - name: 构建 OpenCV Matcher (Python)
        shell: cmd
        working-directory: opencv-matcher
        run: |
          echo === 构建 tke-opencv for Windows ===
          echo 版本号: %BUILD_VERSION%

          REM 生成 _version.py 文件（打包时嵌入版本号）
          echo # 自动生成的版本文件 - 请勿手动修改 > _version.py
          echo __version__ = '%BUILD_VERSION%' >> _version.py
          echo 已生成 _version.py: %BUILD_VERSION%

          REM 同步依赖
          uv sync --group dev

          REM 打包
          .venv\Scripts\pyinstaller --onefile --name tke-opencv --clean --noconfirm opencv_matcher.py

          REM 清理生成的 _version.py
          if exist "_version.py" del /f /q "_version.py"

          REM 检查打包是否成功
          if not exist "dist\tke-opencv.exe" (
            echo ❌ 打包失败：未找到 dist\tke-opencv.exe
            exit /b 1
          )

          REM 创建目标目录
          if not exist "..\resources\win32\toolkit-engine" mkdir "..\resources\win32\toolkit-engine"

          REM 复制到资源目录
          copy /Y "dist\tke-opencv.exe" "..\resources\win32\toolkit-engine\tke-opencv.exe"

          echo ✅ tke-opencv 构建完成
          dir "..\resources\win32\toolkit-engine\tke-opencv.exe"

      # 构建 tester-ai (Rust)
      - name: 构建 AI Tester (Rust)
        shell: cmd
        working-directory: tester-ai
        run: |
          echo === 构建 tester-ai for Windows ===
          echo 版本号: %BUILD_VERSION%

          REM 构建（build.rs 会自动从 BUILD_VERSION 环境变量读取版本）
          cargo build --release

          REM 创建目标目录
          if not exist "..\resources\win32\tester-ai" mkdir "..\resources\win32\tester-ai"

          REM 复制到资源目录
          copy /Y "target\release\tester-ai.exe" "..\resources\win32\tester-ai\tester-ai.exe"

          echo ✅ tester-ai 构建完成
          dir "..\resources\win32\tester-ai\tester-ai.exe"

      # 构建 scrcpy-server (Rust)
      - name: 构建 Scrcpy Server (Rust)
        working-directory: scrcpy-server
        shell: cmd
        run: |
          echo === 构建 scrcpy-server for Windows ===
          echo 版本号: %BUILD_VERSION%

          REM 构建（build.rs 会自动从 BUILD_VERSION 环境变量读取版本）
          cargo build --release

          REM 创建目标目录
          if not exist "..\resources\win32\scrcpy-server" mkdir "..\resources\win32\scrcpy-server"

          REM 复制二进制文件
          copy /Y "target\release\tke-scrcpy.exe" "..\resources\win32\scrcpy-server\tke-scrcpy.exe"

          REM 直接复制 scrcpy-server.jar 到二进制文件旁边
          copy /Y "vendor\Genymobile\scrcpy\scrcpy-server.jar" "..\resources\win32\scrcpy-server\scrcpy-server.jar"

          echo ✅ scrcpy-server 构建完成
          dir "..\resources\win32\scrcpy-server\tke-scrcpy.exe"

          REM 验证 jar 文件
          if exist "..\resources\win32\scrcpy-server\scrcpy-server.jar" (
            echo scrcpy-server.jar 已复制
            dir "..\resources\win32\scrcpy-server\scrcpy-server.jar"
          ) else (
            echo ❌ scrcpy-server.jar 未找到
            exit /b 1
          )

      # 验证所有二进制文件
      - name: 验证构建产物
        shell: cmd
        run: |
          echo === 验证构建产物 ===

          echo 📦 TKE:
          resources\win32\toolkit-engine\tke.exe --version
          if errorlevel 1 exit /b 1

          echo 📦 tke-opencv:
          resources\win32\toolkit-engine\tke-opencv.exe --version
          if errorlevel 1 exit /b 1

          echo 📦 tester-ai:
          resources\win32\tester-ai\tester-ai.exe --version
          if errorlevel 1 exit /b 1

          echo 📦 tke-scrcpy:
          resources\win32\scrcpy-server\tke-scrcpy.exe --version
          if errorlevel 1 (
            echo 尝试显示帮助信息...
            resources\win32\scrcpy-server\tke-scrcpy.exe --help
          )

          echo 📦 scrcpy-server.jar:
          dir resources\win32\scrcpy-server\scrcpy-server.jar

          echo ✅ 所有二进制文件验证完成

      # 下载 FFmpeg 依赖（Windows）
      - name: 下载 FFmpeg
        shell: pwsh
        run: |
          Write-Host "=== 下载 Windows FFmpeg ==="

          $ffmpegUrl = "https://toolkit-studio-updates.s3.ap-southeast-2.amazonaws.com/dependency/win32/ffmpeg.exe"
          $targetDir = "resources\win32\toolkit-engine"
          $targetFile = "$targetDir\ffmpeg.exe"

          # 确保目标目录存在
          if (-not (Test-Path $targetDir)) {
            New-Item -ItemType Directory -Force -Path $targetDir | Out-Null
          }

          # 下载 ffmpeg.exe
          Write-Host "下载 ffmpeg.exe 从: $ffmpegUrl"
          try {
            Invoke-WebRequest -Uri $ffmpegUrl -OutFile $targetFile -ErrorAction Stop
            Write-Host "✅ ffmpeg.exe 下载成功"

            # 验证文件
            $fileInfo = Get-Item $targetFile
            Write-Host "文件大小: $([math]::Round($fileInfo.Length / 1MB, 2)) MB"

          } catch {
            Write-Host "❌ 下载失败: $_"
            exit 1
          }

      # 构建 Electron 应用 (Windows)
      - name: 构建 Electron 应用
        shell: pwsh
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ap-southeast-2
        run: |
          Write-Host "=== 构建 Electron 应用 for Windows ==="
          # electron-builder 只在检测到 git tag 时才会上传到 S3
          npx electron-builder --win

          if ("${{ github.event_name }}" -eq "push") {
            Write-Host "✅ Electron 应用构建完成并已上传到 S3"
          } else {
            Write-Host "✅ Electron 应用构建完成（未上传到 S3，仅测试构建）"
          }
          Get-ChildItem dist\

      # 重命名应用
      - name: 重命名应用
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path release

          # 查找生成的安装包
          $exeFile = Get-ChildItem -Path dist -Filter "*.exe" -Recurse | Select-Object -First 1
          if ($exeFile) {
            Copy-Item -Path $exeFile.FullName -Destination "release/Toolkit Studio ${{ needs.prepare.outputs.tag }} Windows.exe"
            Write-Host "✅ 找到安装包: $($exeFile.FullName)"
          } else {
            Write-Host "❌ 未找到安装包"
            Get-ChildItem -Path dist -Recurse
            exit 1
          }

      # 上传到 Artifacts
      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: windows-app
          path: release/*
          retention-days: 30

  # 创建 GitHub Release（仅在 tag 触发时）
  create-release:
    name: 创建 GitHub Release
    needs: [prepare, build-macos, build-windows]
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'push' &&
      (needs.build-macos.result == 'success' || needs.build-windows.result == 'success')
    permissions:
      contents: write
    steps:
      - name: 下载所有 Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: 列出所有文件
        run: |
          echo "📦 所有构建产物:"
          find artifacts -type f -exec ls -lh {} \;

      - name: 创建 Release 草稿
        uses: softprops/action-gh-release@v1
        with:
          draft: true
          tag_name: ${{ needs.prepare.outputs.tag }}
          name: Toolkit Studio ${{ needs.prepare.outputs.tag }}
          generate_release_notes: true
          files: artifacts/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # 后续处理：上传 Release Notes 和更新 package.json
  post-build:
    name: Bot更新版本信息
    needs: [prepare, build-macos, build-windows]
    runs-on: ubuntu-latest
    environment: aws-production
    if: |
      needs.build-macos.result == 'success' || needs.build-windows.result == 'success'
    permissions:
      contents: write
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 更新Release Note索引文件和package.json版本号
        if: github.event_name == 'push'
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ap-southeast-2
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"

          # 1. 上传 Release Notes
          if [ -f "build/RELEASE_NOTES.md" ]; then
            echo "📝 上传 RELEASE_NOTES.md 到 S3..."
            aws s3 cp build/RELEASE_NOTES.md s3://toolkit-studio-updates/release-notes/${VERSION}.md --content-type "text/markdown"
            echo "✅ Release notes 已上传: s3://toolkit-studio-updates/release-notes/${VERSION}.md"

            # 2. 更新 Release Notes 索引
            echo ""
            echo "📋 更新 Release Notes 索引..."
            INDEX_FILE="release-note-index.json"
            S3_INDEX_PATH="s3://toolkit-studio-updates/release-notes/${INDEX_FILE}"

            # 下载现有索引文件（如果存在）
            if aws s3 cp "${S3_INDEX_PATH}" "${INDEX_FILE}" 2>/dev/null; then
              echo "✓ 已下载现有索引文件"
            else
              echo "✓ 索引文件不存在，创建新索引"
              echo '{"versions":[]}' > "${INDEX_FILE}"
            fi

            # 获取当前时间戳（ISO 8601 格式）
            TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

            # 安装 jq（如果未安装）
            if ! command -v jq &> /dev/null; then
              echo "安装 jq..."
              sudo apt-get update -qq && sudo apt-get install -y -qq jq
            fi

            # 检查版本是否已存在
            EXISTING=$(jq --arg ver "$VERSION" '.versions[] | select(.version == $ver)' "${INDEX_FILE}")

            if [ -z "$EXISTING" ]; then
              echo "✓ 添加新版本: $VERSION"
              # 添加新版本到数组开头（最新的在前面）
              jq --arg ver "$VERSION" \
                 --arg date "$TIMESTAMP" \
                 --arg file "${VERSION}.md" \
                 '.versions = [{version: $ver, date: $date, file: $file}] + .versions' \
                 "${INDEX_FILE}" > "${INDEX_FILE}.tmp"
            else
              echo "✓ 更新现有版本: $VERSION"
              # 更新现有版本的时间戳
              jq --arg ver "$VERSION" \
                 --arg date "$TIMESTAMP" \
                 --arg file "${VERSION}.md" \
                 '(.versions[] | select(.version == $ver)) |= {version: $ver, date: $date, file: $file}' \
                 "${INDEX_FILE}" > "${INDEX_FILE}.tmp"
            fi

            mv "${INDEX_FILE}.tmp" "${INDEX_FILE}"

            # 显示更新后的索引（只显示前3个版本）
            echo "索引文件内容（最新3个版本）:"
            jq '.versions | .[0:3]' "${INDEX_FILE}"

            # 保存到 build 目录（供后续提交到 git）
            mkdir -p build
            cp "${INDEX_FILE}" "build/${INDEX_FILE}"
            echo "✓ 索引文件已保存到 build/${INDEX_FILE}"

            # 上传索引到 S3
            aws s3 cp "${INDEX_FILE}" "${S3_INDEX_PATH}" --content-type "application/json"
            echo "✅ 索引文件已上传: ${S3_INDEX_PATH}"
          else
            echo "⚠️  未找到 build/RELEASE_NOTES.md，跳过上传和索引更新"
          fi

      - name: 提交git commit
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"

          # 读取当前 package.json 中的版本
          CURRENT_VERSION=$(grep '"version"' package.json | head -1 | sed -E 's/.*"version": *"([^"]+)".*/\1/')

          # 检查是否有文件需要提交
          HAS_CHANGES=false

          # 检查 package.json 是否需要更新
          if [[ "$CURRENT_VERSION" != "$VERSION" ]]; then
            echo "更新 package.json: $CURRENT_VERSION → $VERSION"
            sed -i -E "s/\"version\": *\"[^\"]+\"/\"version\": \"$VERSION\"/" package.json
            HAS_CHANGES=true
          else
            echo "✓ package.json 版本已是最新: $VERSION"
          fi

          # 检查 release-note-index.json 是否存在（从前面步骤生成）
          if [ -f "build/release-note-index.json" ]; then
            echo "✓ 发现更新的 release-note-index.json"
            HAS_CHANGES=true
          fi

          # 如果有任何更改，提交到 git
          if [ "$HAS_CHANGES" = true ]; then
            # 配置 git
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"

            # 添加文件到 git
            git add package.json build/release-note-index.json 2>/dev/null || true

            # 检查是否真的有变更
            if git diff --staged --quiet; then
              echo "✓ 没有实际变更需要提交"
            else
              # 提交更改并推送到 main 分支
              git commit -m "Update: version $VERSION and release notes index by GitHub Action"
              git push

              echo "✅ 版本信息已更新并提交到 main 分支"
              git show --stat HEAD
            fi
          else
            echo "✅ 所有版本信息已是最新，无需更新"
          fi
